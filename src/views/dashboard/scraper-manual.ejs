<!-- Manual Scraping Page -->
<div class="container-fluid px-4">
    <h1 class="mt-4">Manual Scraping</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a href="/dashboard">Dashboard</a></li>
        <li class="breadcrumb-item"><a href="/dashboard/scraper">News Scraper</a></li>
        <li class="breadcrumb-item active">Manual Scraping</li>
    </ol>

    <div class="row">
        <div class="col-xl-12">
            <div class="card mb-4">
                <div class="card-header">
                    <i class="fas fa-robot me-1"></i>
                    Manual Scraping: <%= scraper.name %>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <i class="fas fa-info-circle me-2"></i>
                        Manual scraping allows you to run a scraper and see detailed progress of each step in the process.
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-6">
                            <div class="card bg-light">
                                <div class="card-body">
                                    <h5 class="card-title">Scraper Details</h5>
                                    <div class="mb-3">
                                        <strong>ID:</strong> <%= scraper.id %>
                                    </div>
                                    <div class="mb-3">
                                        <strong>Name:</strong> <%= scraper.name %>
                                    </div>
                                    <% if (scraper.website_name) { %>
                                        <div class="mb-3">
                                            <strong>Website:</strong> 
                                            <% if (scraper.website_url) { %>
                                                <a href="<%= scraper.website_url %>" target="_blank">
                                                    <%= scraper.website_name %> <i class="fas fa-external-link-alt fa-xs"></i>
                                                </a>
                                            <% } else { %>
                                                <%= scraper.website_name %>
                                            <% } %>
                                        </div>
                                    <% } %>
                                    <% if (scraper.description) { %>
                                        <div class="mb-3">
                                            <strong>Description:</strong> <%= scraper.description %>
                                        </div>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="card bg-light h-100">
                                <div class="card-body">
                                    <h5 class="card-title">Scraping Process</h5>
                                    <p>When you start the manual scraping process, you will see detailed information about:</p>
                                    <ol>
                                        <li>Getting list of news articles with titles and URLs</li>
                                        <li>Scraping each news article individually</li>
                                        <li>Processing HTML content</li>
                                        <li>Analyzing and extracting structured data</li>
                                        <li>Saving processed articles</li>
                                    </ol>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        <strong>Warning:</strong> Manual scraping may take several minutes depending on the number of articles found.
                    </div>

                    <form action="/dashboard/scraper/<%= scraper.id %>/manual" method="POST" class="mt-4">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a href="/dashboard/scraper" class="btn btn-secondary me-md-2">
                                <i class="fas fa-arrow-left"></i> Back to Scrapers
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-play"></i> Start Manual Scraping
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div> 